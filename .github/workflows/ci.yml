name: ci

on:
  # Only run CI on:
  #   - pushes to main (so we catch a broken main fast)
  #   - PRs targeting main (so we catch breakage before merge)
  # Pushes to non-main branches don't run CI by themselves; the PR run covers them.
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  typecheck-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - run: npm ci

      - name: Type-check Worker
        run: npx tsc -p tsconfig.json --noEmit

      - name: Type-check + build web
        run: npm -w web run build

      - name: Bundle Worker (dry-run, validates wrangler.toml)
        run: npx wrangler deploy --dry-run --outdir .wrangler/tmp

      - name: Run tests
        run: npm test

  # Pure-C CLI (cli/): build with libcurl + corec arenas, then snapshot-test
  # against a local fixture server (no Cloudflare / no network).
  cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake \
            ninja-build \
            libcurl4-openssl-dev \
            pkg-config

      - name: Configure
        run: cmake -S cli -B cli/build -G Ninja -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build cli/build

      - name: Reference tests
        run: cli/tests/run_tests.sh cli/build/githost
